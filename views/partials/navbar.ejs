<% 
// Navbar partial: permisos + plan. Solo se muestra enlace si el usuario tiene permiso Y el plan del tenant incluye el módulo.
const isSuperadmin = user && String(user.rol || '').toLowerCase() === 'superadmin';
const brandHref = isSuperadmin ? '/admin/dashboard' : '/';
const planLabels = { dashboard: 'Dashboard', productos: 'Productos', clientes: 'Clientes', mesas: 'Mesas', cocina: 'Cocina', ventas: 'Ventas', configuracion: 'Configuración', inventario: 'Inventario', recetas: 'Recetas', eventos: 'Eventos', analitica: 'Analítica', prediccion_ml: 'Predicción', plantillas: 'Plantillas', importar_exportar: 'Import/Export', costeo: 'Costeo' };
const ap = typeof allowedByPlan !== 'undefined' && allowedByPlan ? allowedByPlan : {};
function canShow(mod) { if (!mod) return true; if (typeof allowedByPlan === 'undefined') return true; return ap[mod] === true; }
%>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="<%= brandHref %>">
            <i class="bi bi-shop"></i> <%= typeof tenant !== 'undefined' && tenant ? tenant.nombre : 'Sistema Restaurante' %>
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <% if (isSuperadmin) { %>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentAdminPage !== 'undefined' && currentAdminPage === 'dashboard') ? 'active' : '' %>" href="/admin/dashboard">
                        <i class="bi bi-speedometer2"></i> Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentAdminPage !== 'undefined' && currentAdminPage === 'tenants') ? 'active' : '' %>" href="/admin/tenants">
                        <i class="bi bi-building"></i> Restaurantes
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentAdminPage !== 'undefined' && currentAdminPage === 'sistema') ? 'active' : '' %>" href="/admin/sistema">
                        <i class="bi bi-gear-wide-connected"></i> Sistema
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentAdminPage !== 'undefined' && currentAdminPage === 'planes') ? 'active' : '' %>" href="/admin/planes">
                        <i class="bi bi-layers"></i> Planes
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentAdminPage !== 'undefined' && currentAdminPage === 'permisos') ? 'active' : '' %>" href="/admin/permisos">
                        <i class="bi bi-shield-lock"></i> Permisos
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (typeof currentAdminPage !== 'undefined' && currentAdminPage === 'ventas') ? 'active' : '' %>" href="/admin/ventas">
                        <i class="bi bi-trash"></i> Eliminar ventas
                    </a>
                </li>
                <% } else { %>
                <% if (user && user.rol === 'admin' && canShow('dashboard')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/dashboard">
                        <i class="bi bi-speedometer2"></i> Dashboard
                    </a>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/">
                        <i class="bi bi-house"></i> Inicio
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('productos.ver') && canShow('productos')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/productos">
                        <i class="bi bi-box-seam"></i> Productos
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('clientes.ver') && canShow('clientes')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/clientes">
                        <i class="bi bi-people"></i> Clientes
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('mesas.ver') && canShow('mesas')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/mesas">
                        <i class="bi bi-table"></i> Mesas
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('cocina.ver') && canShow('cocina')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/cocina">
                        <i class="bi bi-fire"></i> Cocina
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('ventas.ver') && canShow('ventas')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/ventas">
                        <i class="bi bi-graph-up"></i> Ventas
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('configuracion.ver') && canShow('configuracion')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/configuracion">
                        <i class="bi bi-gear"></i> Configuración
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('eventos.ver') && canShow('eventos')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/eventos">
                        <i class="bi bi-calendar-event"></i> Eventos
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('inventario.ver') && canShow('inventario')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/inventario">
                        <i class="bi bi-boxes"></i> Inventario
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('recetas.ver') && canShow('recetas')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/recetas">
                        <i class="bi bi-journal-text"></i> Recetas
                    </a>
                </li>
                <% } %>
                <% if (user && user.permisos && user.permisos.includes('analitica.ver') && canShow('analitica')) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/analitica">
                        <i class="bi bi-graph-up-arrow"></i> Analítica
                    </a>
                </li>
                <% } %>
                <% } %>
            </ul>
            
            <ul class="navbar-nav">
                <% if (user) { %>
                <% if (!isSuperadmin && typeof tenant !== 'undefined' && tenant && (tenant.plan || tenant.plan_nombre) && typeof plans !== 'undefined' && plans && plans.length) { %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="planDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-layers me-1"></i>
                        <span class="badge bg-primary"><%= tenant.plan ? tenant.plan.nombre : (tenant.plan_nombre || 'Plan') %></span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end p-3" style="min-width: 320px;">
                        <li class="dropdown-header mb-2">Tu plan actual: <strong><%= tenant.plan ? tenant.plan.nombre : (tenant.plan_nombre || '') %></strong></li>
                        <li><hr class="dropdown-divider"></li>
                        <% plans.forEach(p => { %>
                        <li class="mb-2">
                            <div class="card border-0 shadow-sm <%= (tenant.plan && tenant.plan.id === p.id) || (tenant.plan_id == p.id) ? 'border-primary border-2' : '' %>">
                                <div class="card-body py-2 px-3">
                                    <strong><%= p.nombre %></strong>
                                    <% if ((tenant.plan && tenant.plan.id === p.id) || (tenant.plan_id == p.id)) { %><span class="badge bg-success ms-1">Actual</span><% } %>
                                    <ul class="list-unstyled small mb-0 mt-1 text-muted">
                                        <% (p.caracteristicas || []).slice(0, 5).forEach(c => { %>
                                        <li><i class="bi bi-check text-success"></i> <%= typeof planLabels !== 'undefined' && planLabels[c] ? planLabels[c] : c %></li>
                                        <% }); %>
                                    </ul>
                                </div>
                            </div>
                        </li>
                        <% }); %>
                    </ul>
                </li>
                <% } %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle"></i> 
                        <%= user.nombre_completo || user.username %>
                        <span class="badge bg-secondary ms-1"><%= user.rol %></span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <h6 class="dropdown-header">
                                <%= user.email || 'Sin email' %>
                            </h6>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item" href="/auth/cambiar-password">
                                <i class="bi bi-key"></i> Cambiar contraseña
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="/auth/logout">
                                <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
                            </a>
                        </li>
                    </ul>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/auth/login">
                        <i class="bi bi-box-arrow-in-right"></i> Iniciar Sesión
                    </a>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

